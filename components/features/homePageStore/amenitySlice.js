import {createSlice} from "@reduxjs/toolkit";

const initialState = {};

const amenitySlice = createSlice({
	name: "action related to amenity of the property",
	initialState,
	reducers: {},
});

export const {} = amenitySlice.actions;

export default amenitySlice.reducer;
