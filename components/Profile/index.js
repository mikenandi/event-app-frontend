import {View, Text, StyleSheet} from "react-native";

export default function Profile(props) {
	return (
		<View>
			<Text>this is the profile area</Text>
		</View>
	);
}
const styles = StyleSheet.create({
	screen: {flex: 1},
});
